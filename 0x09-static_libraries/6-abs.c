#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @n: function parameter
 * Return: int
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
